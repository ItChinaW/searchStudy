import type {NextPage} from 'next'
import Head from 'next/head'

const Home: NextPage = ({}) => {
    return (
        <div>
            <Head>
                <title>首页</title>
            </Head>
        </div>
    )
}

export default Home
